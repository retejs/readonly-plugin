{
  "name": "rete-readonly-plugin",
  "version": "0.1.1",
  "description": "",
  "main": "build/readonly-plugin.min.js",
  "scripts": {
    "build": "rete --build rete.config.js",
    "build:dev": "rete --build rete.config.js --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "rete-cli": "^0.1.0"
  }
}
